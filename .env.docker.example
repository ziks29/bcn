# Environment Configuration for Docker

# Copy this file to .env.docker for Docker-specific configuration
# MongoDB Atlas - Cloud Database

# Database Configuration (MongoDB Atlas)
# Get your connection string from MongoDB Atlas: https://cloud.mongodb.com
# Replace <username>, <password>, <cluster>, and <database> with your actual values
DATABASE_URL="mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority"

# Next Auth Configuration
NEXTAUTH_SECRET="super-secret-development-key-change-in-prod"
NEXTAUTH_URL="https://blainenews.n9xo.xyz"

# Production Notes:
# - Change NEXTAUTH_SECRET to a secure random string (use: openssl rand -base64 32)
# - Update NEXTAUTH_URL to your production domain
# - MongoDB Atlas provides automatic backups and scaling
# - No local database files needed - all data stored in Atlas
# - Port 9999 is exposed in docker-compose.yml for production

# GitHub Container Registry
# Replace with your actual GitHub username/repo (e.g., yourusername/bcn)
GITHUB_REPOSITORY=your-username/bcn
